# ==================================================
# LOCAL DEVELOPMENT ENVIRONMENT (EXAMPLE)
# ==================================================
# Copy this file to .env.local and update with your local Supabase keys
#
# SETUP:
#   1. Start local Supabase: supabase start
#   2. Get keys: supabase status
#   3. Copy this file: cp .env.local.example .env.local
#   4. Update keys below with values from: supabase status
#   5. Run services:
#      - Frontend: npm run dev
#      - Backend: docker-compose up
#
# .env.local is gitignored and will override .env and .env.development
# ==================================================

# --------------------------------------------------
# Frontend (Vite) - Supabase Configuration (LOCAL)
# --------------------------------------------------
# Start local Supabase: supabase start
# Get keys: supabase status (look for "anon key" or "publishable key")
# Local Supabase outputs new-format keys: sb_publishable_...

VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_PUBLISHABLE_KEY=YOUR_LOCAL_PUBLISHABLE_KEY_HERE

# --------------------------------------------------
# Frontend (Vite) - Application URL
# --------------------------------------------------
# For local development, use localhost. For production, use your custom domain.
VITE_APP_URL=http://localhost:5173

# --------------------------------------------------
# Frontend (Vite) - Flask API URL
# --------------------------------------------------
VITE_FLASK_API_URL=http://localhost:5000

# --------------------------------------------------
# Backend (Flask) - Supabase Configuration (LOCAL)
# --------------------------------------------------
# Get secret key: supabase status (look for "service_role key" or "secret key")
# Local Supabase outputs new-format keys: sb_secret_...
# NOTE: Use localhost for dev containers, host.docker.internal for docker-compose

SUPABASE_URL=http://localhost:54321
SUPABASE_SECRET_KEY=YOUR_LOCAL_SECRET_KEY_HERE

# --------------------------------------------------
# OpenAI (Optional - use mock for local dev)
# --------------------------------------------------
# VITE_OPENAI_API_KEY=mock
