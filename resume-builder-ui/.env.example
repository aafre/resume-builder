# ==================================================
# FRONTEND CONFIGURATION (React/Vite)
# ==================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to git!

# --------------------------------------------------
# Supabase Configuration (Frontend)
# --------------------------------------------------
# Project URL - Find in: Supabase Dashboard -> Settings -> API -> Project URL
VITE_SUPABASE_URL=https://your-project.supabase.co

# Anon/Public Key - Find in: Supabase Dashboard -> Settings -> API -> "anon public" key
# This key is safe to use in a browser if you have Row Level Security enabled
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# --------------------------------------------------
# NOTE: Vite Environment Variables
# --------------------------------------------------
# Vite only exposes variables prefixed with VITE_ to the browser
# These values are embedded into the build at compile time
# Never put secret keys here - they will be visible in the built JavaScript
