# ==================================================
# FRONTEND CONFIGURATION (React/Vite)
# ==================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to git!

# --------------------------------------------------
# Supabase Configuration (Frontend)
# --------------------------------------------------
# Project URL - Find in: Supabase Dashboard -> Settings -> API -> Project URL
VITE_SUPABASE_URL=https://your-project.supabase.co

# Publishable API Key - Find in: Supabase Dashboard -> Settings -> API -> "Publishable API Key"
# This key is safe to use in a browser if you have Row Level Security enabled
# Format: sb_publishable_... (new format required)
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your-publishable-key-here

# --------------------------------------------------
# Application Configuration
# --------------------------------------------------
# Base URL for the application (used for sitemap generation, etc.)
VITE_APP_URL=https://your-domain.com

# --------------------------------------------------
# Ads Configuration
# --------------------------------------------------
# Explicit ad placements (in-content, in-feed, sidebar)
# Set to 'true' to enable. Default: disabled (Auto Ads only)
# Before enabling: create ad units in AdSense and update slot IDs
VITE_ENABLE_EXPLICIT_ADS=false

# --------------------------------------------------
# Affiliate / Jobs Configuration
# --------------------------------------------------
# Job search feature (Adzuna integration)
# Set to 'true' to show Jobs nav links and search UI
VITE_AFFILIATE_JOB_SEARCH_ENABLED=false

# Resume review affiliate (FlexOffers integration)
# Set to 'true' to show ATS review prompts
VITE_AFFILIATE_RESUME_REVIEW_ENABLED=false

# --------------------------------------------------
# NOTE: Vite Environment Variables
# --------------------------------------------------
# Vite only exposes variables prefixed with VITE_ to the browser
# These values are embedded into the build at compile time
# Never put secret keys here - they will be visible in the built JavaScript
